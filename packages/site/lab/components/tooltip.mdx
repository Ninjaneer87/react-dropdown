import TooltipUsage from '@site/src/components/lab/Tooltip/TooltipUsage';
import TooltipPlacements from '@site/src/components/lab/Tooltip/TooltipPlacements';
import TooltipControlled from '@site/src/components/lab/Tooltip/TooltipControlled';
import TooltipDelayShow from '@site/src/components/lab/Tooltip/TooltipDelayShow';
import TooltipDelayHide from '@site/src/components/lab/Tooltip/TooltipDelayHide';
import TooltipHoverableContent from '@site/src/components/lab/Tooltip/TooltipHoverableContent';
import Tabs from '@theme/Tabs';
import TabItem from '@theme/TabItem';
import CodeBlock from '@theme/CodeBlock';
import Link from '@docusaurus/Link';
import { PopoverPlacementTypePopover } from '@site/src/components/site-lab/TypePopover/popovers/shared';
import { PopoverSizeTypePopover } from '@site/src/components/site-lab/TypePopover/popovers/popover';
import { TooltipClassNamesTypePopover } from '@site/src/components/site-lab/TypePopover/popovers/tooltip';
import {
  tooltipMyTooltipTsx,
  tooltipMyTooltipScss,
  tooltipControlledTsx,
  tooltipPlacementsTsx,
  tooltipUsageTsx,
  tooltipDelayShowTsx,
  tooltipDelayHideTsx,
  tooltipHoverableContentTsx,
} from '@site/lab/components/_snippets/tooltip';
import SourceLink from '@site/src/components/shared/SourceLink';

# Tooltip

<SourceLink entity="component" name="Tooltip" />

A Tooltip is a small, non-interactive overlay that displays brief informational text when hovering over or focusing on an element. It is built on top of the <Link to='/lab/components/popover'>Popover</Link> component.

## Key Characteristics

- **Hover-Triggered**: Automatically shows on hover with configurable delay.
- **Non-Interactive**: Unlike Popover, tooltips are meant for displaying simple text hints, not interactive content. Still it's possible to interact with the tooltip content if/when needed via `hoverableContent` prop.
- **Accessible**: Works with keyboard focus and follows accessibility best practices.

## Import

```tsx
import { Tooltip } from '@andrejground/lab';
```

## Customization

Add your styles and your component is ready to be used.

<div className="code-tabs">
  <Tabs>
    <TabItem value="MyTooltip.tsx" label="MyTooltip.tsx" default>
      <CodeBlock language="tsx">{tooltipMyTooltipTsx}</CodeBlock>
    </TabItem>
    <TabItem value="MyTooltip.module.scss" label="MyTooltip.module.scss">
      <CodeBlock language="scss">{tooltipMyTooltipScss}</CodeBlock>
    </TabItem>
  </Tabs>
</div>

## Usage

<Tabs>
  <TabItem value="preview" label="Preview" default className="preview-tab">
    <TooltipUsage />
  </TabItem>
  <TabItem value="code" label="Code">
    <CodeBlock language="tsx">{tooltipUsageTsx}</CodeBlock>
  </TabItem>
</Tabs>

## Placement

<Tabs>
  <TabItem value="preview" label="Preview" default className="preview-tab">
    <TooltipPlacements />
  </TabItem>
  <TabItem value="code" label="Code">
    <CodeBlock language="tsx">{tooltipPlacementsTsx}</CodeBlock>
  </TabItem>
</Tabs>

## Delay Show

Control the delay before showing the tooltip with `delayShow` prop.

<Tabs>
  <TabItem value="preview" label="Preview" default className="preview-tab">
    <TooltipDelayShow />
  </TabItem>
  <TabItem value="code" label="Code">
    <CodeBlock language="tsx">{tooltipDelayShowTsx}</CodeBlock>
  </TabItem>
</Tabs>

## Delay Hide

Control the delay before hiding the tooltip with `delayHide` prop.

<Tabs>
  <TabItem value="preview" label="Preview" default className="preview-tab">
    <TooltipDelayHide />
  </TabItem>
  <TabItem value="code" label="Code">
    <CodeBlock language="tsx">{tooltipDelayHideTsx}</CodeBlock>
  </TabItem>
</Tabs>

## Hoverable Content

Use `hoverableContent` prop to keep the tooltip visible when hovering over the tooltip content.

<Tabs>
  <TabItem value="preview" label="Preview" default className="preview-tab">
    <TooltipHoverableContent />
  </TabItem>
  <TabItem value="code" label="Code">
    <CodeBlock language="tsx">{tooltipHoverableContentTsx}</CodeBlock>
  </TabItem>
</Tabs>

## Controlled

<Tabs>
  <TabItem value="preview" label="Preview" default className="preview-tab">
    <TooltipControlled />
  </TabItem>
  <TabItem value="code" label="Code">
    <CodeBlock language="tsx">{tooltipControlledTsx}</CodeBlock>
  </TabItem>
</Tabs>

## API

### Tooltip Props

| Prop | Type | Default | Description |
|------|------|---------|-------------|
| `children` | `ReactNode` | - | The element that triggers the tooltip |
| `content` | `ReactNode` | **required** | Content displayed inside the tooltip |
| `placement` | <PopoverPlacementTypePopover /> | `"top-center"` | Preferred placement of the tooltip relative to the trigger |
| `offset` | `number` | `8` | Distance in pixels between the tooltip and the trigger |
| `size` | <PopoverSizeTypePopover /> | `"small"` | Controls the width of the tooltip |
| `isOpen` | `boolean` | - | Controls the tooltip open state (controlled mode) |
| `isDisabled` | `boolean` | - | Disables the tooltip |
| `showArrow` | `boolean` | `true` | Shows an arrow pointing toward the trigger |
| `shouldFlip` | `boolean` | `true` | Flips placement when there is not enough space |
| `shouldCloseOnClickOutside` | `boolean` | `true` | Closes the tooltip when clicking outside of it |
| `shouldCloseOnEsc` | `boolean` | `true` | Closes the tooltip when the Escape key is pressed |
| `shouldCloseOnTriggerBlur` | `boolean` | `true` | Closes the tooltip when the trigger loses focus |
| `openOnFocus` | `boolean` | `true` | Opens the tooltip when the trigger receives focus |
| `delayShow` | `number` | `0` | Delay in milliseconds before showing the tooltip |
| `delayHide` | `number` | `hoverableContent ? 200 : 0` | Delay in milliseconds before hiding the tooltip |
| `hoverableContent` | `boolean` | `false` | Keeps the tooltip open while hovering over its content |
| `triggerWrapper` | `boolean` | - | Wraps the trigger in a span instead of using Slot |
| `fullWidthTriggerWrapper` | `boolean` | - | Makes the trigger wrapper take full width |
| `onOpen` | `() => void` | - | Callback fired when the tooltip opens |
| `onClose` | `() => void` | - | Callback fired when the tooltip closes |
| `onClickOutside` | `(event: MouseEvent \| TouchEvent) => void` | - | Callback fired when clicking outside the tooltip |
| `onOpenChange` | `(isOpen: boolean) => void` | - | Callback fired when the open state changes |
| `onTriggerFocus` | `() => void` | - | Callback fired when the trigger receives focus |
| `onTriggerBlur` | `() => void` | - | Callback fired when the trigger loses focus |
| `classNames` | <TooltipClassNamesTypePopover /> | - | Custom class names for the tooltip slots |
